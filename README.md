# mass-scan
# 🦀 mass-scan  **Blazing-fast CLI wrapper for mass XSS + SQLi + Nuclei scanning**   Written in Rust, powered by `dalfox`, `sqlmap` &amp; `nuclei`.   Replace every `FUZZ` token in your URLs with payloads → scan everything in parallel.
